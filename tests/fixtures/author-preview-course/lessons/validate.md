# Validate records

A JSON object can be well formed but fail your application schema. Predict which records pass before running the notebook. Check required fields and value types independently.

[Field rules](rules.html#field-rules) describe this exercise. A passing schema check does not establish that a person’s age is factually correct.
