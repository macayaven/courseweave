# Local pilot acceptance evidence

This is installation and product-behavior evidence, not a learning-outcome study. The exact platform/course commits, retained source archives, selected wheel/sdist hashes, installed interpreter paths and final check results are in `.pilot/receipt.json`. Detailed logs and screenshots are under `.pilot/evidence/`; the one-command start is [documented here](README.md).

## Observed product behavior

The full final product-code Python suite passed 740 tests. Frontend suites passed Lab 72, Learn 167 and Author 133 tests, and all four package typechecks passed. Ten learner/native/recovery browser cases and sixteen Author browser cases passed. Subsequent layout changes received focused browser coverage; a nonblocking startup-restoration fix passed all 73 Lab tests, including a real DockPanel regression. These suites cover canonical v2 schema/DTO parity, closed authored progress requirements, digest binding, optional/excluded activities, privacy revocation/reset, replay filtering, and exact-byte Author conflicts. V1 remains an explicit preview/copy path.

The generic installed suite exercises a separate small course fixture without the Agent Harness Path source checkout, including authenticated Learn, no-provider behavior, a scripts-disabled native HTML reader, notebook/video/source/terminal surfaces, empty Author creation only on Save, and process/credential cleanup failure sentinels. It passes all twelve checks. The installed adapter suite uses an exact committed course archive, external state and the course's own interpreter. Its receipt separates ordinary local synthetic provider replies and tool candidates from the real gateway observations below.

Actual installed JupyterLab checks cover visible S01 SVG/native self-check, approved lesson-to-notebook link interception, explicit bounded lesson scope with solutions omitted, prediction/attempt records, wrong-check feedback, authored hints and declining a revisit without a provider call or progress mutation. A deliberately held local stream allows navigation while the old turn keeps its notebook attribution; the next question uses the reading activity in the same conversation. Native cell Share is bound to one run and excluded from later provider replay. Open guide, Learn Reconnect and hide/reopen retain the same iframe, conversation and draft until the latest selected cell is confirmed. Screenshots distinguish the 1440×1000 journey from the 1000×720 window with its 250-pixel guide rail.

Two additional installed privacy cases exercise reset and deletion with two pending proposals and a completed action. They hold an actual proposal action and authentic old bootstrap/proposal reads across removal, require retired action and completed-action replay to return 404, and check that late results cannot restore authority. The same iframe, visible conversation, composer draft and unsent proposal edit survive a controlled native context failure and Open Guide recovery. Subsequent local-provider payloads exclude the previously confirmed dependent record/question/answer history, while the visible session transcript remains. Each case passes post-stop credential/process/source checks against the selected frozen wheel; receipts are `installed-privacy-final/installed-reset_state.json` and `installed-delete_state.json`. This fills the installed acceptance gap without a product-code change.

Installed Author reload preserves access to the saved course after Jupyter restores its dock layout. A synthetic provider produces a selected-activity Learning candidate; review proves only the selected overview changes, acceptance writes the exact candidate, and a whitespace-only external change triggers an exact-ETag conflict while retaining the local draft. This verifies the installed review path, not live provider tool support.

The narrow layout keeps a readable transcript and reachable composer, with scrolling for additional composer controls. Keyboard focus reaches Send. DOM checks keep incremental transcript announcements off and announce bounded terminal status. No actual screen-reader or other assistive-technology session was performed.

The actual course kernel reports the course `.venv/bin/python` and `.venv` prefix, with only the owned `python3` spec available. Provider, gateway, capability and Jupyter-token environment-presence checks are false. Transport is TCP on `127.0.0.1`, with HMAC-SHA256 and a present message key. Payloads are not encrypted; the native transport warning is documented rather than suppressed. Installed runs scan owned files and rendered artifacts after shutdown and require no retained credentials, no owned processes and no temporary root.

## Real configured gateway observations

The bounded probes used public/synthetic S01 material and one explicitly configured private HTTPS endpoint, with its credential held only in launch-process memory. The public summary omits the workstation-specific provider route. Successful response bodies and safe usage evidence remain in the private `provider-*.json` records.

| Probe | Observed result | Provider duration and reported tokens |
|---|---|---|
| Text-only grounded answer | Correct ordinary-text versus tool-call obligation, distinct call IDs, mock limitation; exact supplied “Read the lesson” label cited; state unchanged | 20,948 ms; 3,500 input / 96 output; one request |
| Streaming answer under `stream-tools-v1`, with no tools in this request | Actual streaming request, correct call-ID pairing explanation and supplied label; state unchanged | 6,460 ms; 3,438 input / 84 output; one request |
| Targeted feedback on saved wrong `every-assistant` check | Correctly identifies the specific misconception and missing call-B result; supplied label cited; state unchanged | 22,392 ms; 4,066 input / 84 output; one request |
| Tool-bearing preference suggestion under `stream-tools-v1` | Provider request failed; no candidate, no state mutation | 25,707 ms wall time; HTTP status, provider duration/usage and cost unavailable |
| Final installed launcher/browser | Visible SVG, explicit scope, correct bounded answer and exact supplied label in assistant text; Ready only after outcome plus terminal success; state unchanged | 19,852 ms; 3,494 input / 69 output; one request |
| Deliberately tiny eight-token output budget | Terminal `truncated` error; no completed answer and no state mutation | 21,936 ms wall time; provider usage/cost unavailable |

The default launcher therefore selects the observed working `text-only-v1` profile. Live tool-call support is unconfirmed/failed for this configuration; successful synthetic tool conformance and installed Author candidate review do not establish live tool support. No genuine provider refusal was observed; refusal handling is fixture-tested only. Cost was not returned. One earlier streaming request completed before its follow-up state call failed; its success receipt was not retained and it is excluded from the table. The final launcher/browser canary passed against the selected wheel; `final-real-browser.json` and `.png` retain its exact artifact, assistant answer, latency, usage and visible UI. Seven completion requests were initiated across all live probes, including the unretained earlier stream and the two error cases.

Gateway request/response and spend/observability retention is separate from CourseWeave's session-only transcript policy. Acceptance does not claim upstream deletion or change shared gateway settings.

## Course and handoff boundaries

Course native unit tests, fresh HTML/SVG generation, local references, dated SOTA references and replay checks are recorded separately. The reference replay passes its governed fixtures; intentional naive fixture failures remain visible. Full HTTP link validation includes soft remote 403s and an S08 Datadog transport-unavailable result, which is not evidence of a dead URL. Original notebook cell identities and output-free sources are preserved; native notebook execution receipts from course validation remain distinct from provider observations. No native live lab was run.

Six required S01/S02 activities are available. Product smoke time is a suggested test budget; study-duration labels remain estimates. Immediate and delayed unaided attempts, assistance records and feedback sequencing are summarized with the historical [course study references](README.md#course-study-references). No learner gains, timing measurements or delayed retention results have been invented. All three decision rulings and their costs if wrong are preserved in [rulings](rulings.md).

Installed launcher inspect/export/reset/delete checks passed against separate synthetic state; reset/delete retain an empty revisioned database and one retry receipt. The designated local study-state directory remained fresh and separate from acceptance state. Artifacts, runtime, launcher, records and evidence remained usable after plan scratch was removed. The receipt also records the original protected-source comparison. Branches/worktrees remained local; independent whole-branch review was the controller's final acceptance step.
