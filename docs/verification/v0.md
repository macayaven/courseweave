# CourseWeave v0 verification

Verified on 2026-09-04 in the isolated CourseWeave release worktree. The real
course was consumed from an exact committed archive in a test-owned temporary
directory. The adapter checkout itself remained read-only.

## Release identities

| Item | Value |
|---|---|
| Platform verified base HEAD | `bf5f8cb7a4cc17ffa37bfada586bbc3798ef195d` |
| Platform verified base tree | `d9eb91099824fbd6f50c1e59ff6d75b46755a1b8` |
| Initial Task 7.2 proof commit | `bcbfba33a4f54ce8c5e9f368b3cdd5b91f492896` |
| Adapter HEAD | `dca20eb6118ef547484198d2982110d3f770649f` |
| Adapter tree | `4c56c4a019f35995cc08ef16a11b0f1e0638dcc3` |
| Adapter `courseweave.json` SHA-256 | `04913b4a6de5673d6b46c2372144fa6662cd88e4b8f5218aeda8043c9c9c3303` |
| Platform `uv.lock` SHA-256 | `f6108ada8882b213d82d017eef8c6f1cdd4de1e9d0d09077f9e74090d77eaf6f` |
| Frontend `pnpm-lock.yaml` SHA-256 | `8b614c7199b08802a69e2db7b35fc9c490bbf1a43cb748f7228a91330894d3a6` |
| Adapter `uv.lock` SHA-256 | `0235a90fe864867e2eafeaf99dc60ec635b68ea52f9447ca5bc3f6b1b3232f6b` |
| Fresh platform wheel SHA-256 | `059d05b2eb4295dc0abe184212df2db66fb8afbcd9dbe151666d29f5082ea425` |

The platform SHA names the immutable production baseline plus the Task 7.2
test/documentation diff. The resulting release-proof commit is the commit that
contains this receipt.

## Environment

| Tool | Version |
|---|---|
| Python used for the installed proof | `3.11.14` |
| JupyterLab in the installed proof | `4.6.3` |
| Jupyter Server in the installed proof | `2.21.0` |
| Node used only by the build/test controller | `v22.22.2` |
| pnpm | `10.32.1` |
| uv | `0.11.19` |
| Playwright | `1.58.2` |

Node was deliberately absent from the child runtime `PATH`. Each installed
workspace used isolated `HOME` and `TMPDIR` roots and an installed package path
under a fresh Python 3.11 virtual environment.

## Installed adapter proof

Discovery was explicit, serial, and independent of the normal Learn/Author
suites:

The path placeholder below must resolve to the immutable adapter commit
`dca20eb6118ef547484198d2982110d3f770649f` named in this record.
`COURSEWEAVE_ADAPTER_ROOT` does not retarget this historical S01/S02 contract to
an arbitrary current course checkout. For the current full-course verifier, use
[the release runbook](../RELEASING.md#5-prove-course-and-state-compatibility).

```text
COURSEWEAVE_ADAPTER_ROOT=/absolute/path/to/pinned-s01-s02-course-checkout \
  COURSEWEAVE_INSTALLED_ADAPTER=1 \
  pnpm --dir frontend exec playwright test --config playwright.config.ts --list
Total: 1 test in 1 file
```

Final command and result:

```text
COURSEWEAVE_ADAPTER_ROOT=/absolute/path/to/pinned-s01-s02-course-checkout \
  pnpm --dir frontend run test:e2e:installed-adapter
1 passed (1.1m)
```

The browser proof built a fresh wheel from the platform worktree for every
workspace, installed it with Python 3.11 and JupyterLab 4.6.3, and exercised
the actual committed 14-module, 62-phase, 112-surface adapter data. It proved:

- usable missing-provider Learn state and deterministic phase selection;
- real S01 lesson HTML and its exact public HTTPS video route;
- notebook opening and context resolution by real cell ID `69a77fc3`;
- prediction persistence, rendered lab Markdown, and relevant Python source;
- the exact offline S01 replay argv was displayed and copied while a PATH
  canary proved it was not executed;
- Reject caused no durable profile or manifest mutation; an edited Accept
  applied once; replaying its exact idempotency key/body changed no state;
- an intentional backend interruption kept the learner draft unsent;
- a second installed process reloaded authoritative prediction, profile, and
  audit state from the same owned course copy;
- teacher chat used one loopback fake OpenAI-compatible streamed response with
  a dummy non-secret API-key canary and no external provider call;
- Author created, edited, deleted, reordered, canonically saved, and reloaded
  modules in the owned copy;
- tokenless bootstrap, exact-owned process cleanup, and credential scans
  remained intact. Finalization stopped and reaped each runtime, waited for its
  stdout/stderr streams, closed its local transports, scanned while the owned
  root still existed, and only then deleted that exact root. The three scans
  covered 20,218, 20,392, and 19,894 files, including isolated
  runtime/home/tmp, course, browser output, and artifacts.

The failure-path harness also proves that cleanup ownership begins immediately
after each successful acquisition, cleanup continues after a later acquisition
or assertion failure, and cleanup failures are surfaced together with the
primary failure. A dummy canary created only from the installed supervisor's
exit event is detected by the post-stop scan; the test then verifies that the
exact owned root is still deleted. All adapter-source Git status, revision,
tree, and archive invocations run with `GIT_OPTIONAL_LOCKS=0`.

Focused review commands and results:

```text
COURSEWEAVE_INSTALLED_WHEEL=1 pnpm --dir frontend exec playwright test \
  --config playwright.config.ts \
  --grep 'resource custody|disables optional Git locks'
3 passed (1.1s)

COURSEWEAVE_INSTALLED_WHEEL=1 pnpm --dir frontend exec playwright test \
  --config playwright.config.ts \
  --grep 'final credential audit detects'
1 passed (18.3s)
```

The public video request was exactly routed to a local test response so this
release proof required no external service. In ordinary use, playback depends
on network access to the public Google Cloud Storage URL; remote availability
and media transfer are outside the zero-network release gate.

## Fresh installed CLI proof

A separate fresh wheel was installed with the same Python/Jupyter versions and
validated the read-only adapter directly:

```text
uv build --wheel --out-dir "$receipt_root/wheel"
python3.11 -m venv "$receipt_root/venv"
uv export --frozen --all-groups --no-emit-project --no-hashes \
  --output-file "$receipt_root/constraints.txt"
uv pip install --python "$receipt_root/venv/bin/python" \
  --constraint "$receipt_root/constraints.txt" "$wheel_path" \
  'jupyterlab==4.6.3' 'jupyter-server==2.21.0'
PYTHONNOUSERSITE=1 PATH="$receipt_root/venv/bin:/usr/bin:/bin" \
  "$receipt_root/venv/bin/courseweave" validate \
  --course-root /absolute/path/to/pinned-s01-s02-course-checkout

Course manifest is valid: modules=14 phases=62 surfaces=112.
installed_courseweave=.../venv/lib/python3.11/site-packages/courseweave/__init__.py
python=3.11.14
jupyterlab=4.6.3
node_in_installed_path=absent
```

## Platform regression matrix

| Exact command | Result |
|---|---|
| `.venv/bin/pytest -q` | 346 passed, 18 subtests passed |
| `pnpm --dir frontend test` | Lab 66, Learn 159, Author 114 passed |
| `pnpm --dir frontend typecheck` | all four workspace projects passed |
| `pnpm --dir frontend build` | Lab, Learn, Author passed; tracked generated output unchanged |
| `.venv/bin/pytest -q tests/test_packaging.py` | 22 passed |
| `pnpm --dir frontend run test:build-determinism` | 2 seam tests passed; 25 generated files matched committed/build-1/build-2 |
| `pnpm --dir frontend install --frozen-lockfile --strict-peer-dependencies` | lock current; already up to date |
| `pnpm --dir frontend run test:e2e` | Learn 7 passed; 16 Author tests skipped by this config; installed suites undiscovered |
| `pnpm --dir frontend run test:e2e:author` | Author 16 passed |
| `pnpm --dir frontend run test:e2e:installed-wheel` | 12 passed in 1.1m |
| dedicated installed-adapter command above | 1 passed |

## Adapter and course receipts

The adapter's own tests were run without bytecode writes:

```text
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B \
  -m unittest tests.test_courseweave_manifest -v
Ran 20 tests in 2.418s
OK
before_head=dca20eb6118ef547484198d2982110d3f770649f
after_head=dca20eb6118ef547484198d2982110d3f770649f
before_status_bytes=0
after_status_bytes=0
```

Task 7.1 had already run the content-heavy gates in its owned adapter checkout,
so Task 7.2 composed that recorded evidence instead of rebuilding or executing
course content in the committed adapter:

| Recorded Task 7.1 command | Result |
|---|---|
| `uv run python lessons/build.py` plus generated diff | 16 pages; no drift |
| `uv run python lessons/check_sota_urls.py` | 104 tagged rows with URLs |
| `uv run python lessons/check_links.py` | 16 pages; 183 relative references valid |
| `uv run python -m unittest labs/test_contracts.py` | 15 passed |
| `uv run python labs/run.py --all --replay` | governed 9/9; naive 3/9; exit 0 |
| per-file `jupyter nbconvert --execute` | 12 notebooks passed |
| notebook hygiene | 12 output-free notebooks; no missing cell IDs |

Those course gates used Python 3.12.13, JupyterLab 4.6.3, nbconvert 7.17.1,
nbformat 5.11.0, and the adapter lock hash recorded above.

## Checkout custody

The adapter finished at the exact HEAD/tree/manifest hash above with empty
porcelain status. Before the first fallible workspace acquisition in each
mutable owned-copy flow, the browser test registers a failure-safe source
verification cleanup. That cleanup runs after workspace finalization whether
the flow passes or fails. The outer owned-copy boundary repeats direct and
helper source verification and deletes the copy; if the test and cleanup both
fail, both failures are reported with the test error retained as the cause.

The protected canonical source checkout was never opened, built, tested, or
written by the Task 7.2 test runner. Its controller-held preflight snapshot is:

- HEAD `cba27be85558701ea8861de4ef231d9cf40a2f2c`, branch `main`, tracking
  `origin/main`;
- exact dirty status: `.gitignore`, `labs/trivia_host/engine.py`,
  `labs/trivia_host/loop.py`, `labs/trivia_host/tools.py`, and
  `notebooks/s01_agent_loop_toy.ipynb`;
- corresponding SHA-256 values:
  `ab298c7c32259a32a06ac3f085ee3ef4fc26311349277f79f1a0d14214bd043e`,
  `21165e010231b41bfa471694b5d768bc7030dbd0f8aaa44a9b2e3a047b85df43`,
  `4059ed1fc4f0661d9d58d874e7df75ecc6eebc2af0ddc4392e51d9d26709b3fd`,
  `3691ec629ae9e90d704ac48a1030756024a39fa1d9d88ad1340a52bea83f861f`,
  and `1ff856b73abd988609cea0febae5d6b1bf64c4a1e78b457b9fad1e01241f5525`.

The controller performed the final read-only comparison because the release
runner has an explicit no-access boundary: MATCH. HEAD, branch, tracking,
single-worktree registration, and all five mode/size/SHA-256 tuples match the
preflight snapshot exactly. The final porcelain-v1 `-z` byte stream SHA-256 is
`4eb7b399ae66b8bef6ac25cf127d5f2e36807e31bc99925916828739836f8eb4`.
No protected-path write, build, or test was performed.
